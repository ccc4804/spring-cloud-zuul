server:
  port: 8600

spring:
  application:
    name: service-b

#ribbon:
#  eureka:
#    enabled: true
#
eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8300/eureka